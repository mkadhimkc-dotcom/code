/**
 * render.js
 *
 * This module is intended to house logic for dynamically
 * constructing workout cards from data structures. At the moment
 * the application uses static HTML defined in index.html. If you
 * decide to make the program data-driven you could import your
 * workouts from `workouts.js` or `data/workouts.json` and produce
 * DOM nodes here. See `navigation.js` and `schedule.js` for
 * reference implementations of DOM manipulation in ES modules.
 */

export function renderWorkouts(/* data */) {
  // no-op: dynamic rendering not yet implemented
}